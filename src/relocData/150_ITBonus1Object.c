/* relocData file 150: ITBonus1Object — raw blob (pending structural decomposition). */

#include "relocdata_types.h"

u8 dITBonus1Object[0x1180] = {
	#include <ITBonus1Object/ITBonus1Object.data.inc.c>
};
