# Relocation metadata for file 205 (JP variant)
# Format: <type> <ptr_label> <target_label>
# JP build compiles MMarioMainMotion as a single chain-encoded raw u8 blob
# so fixRelocChain has no per-symbol patches to apply here.
