# Relocation metadata for file 249 (JP variant)
# Format: <type> <ptr_label> <target_label>
# JP build compiles BossMainMotion as a single chain-encoded raw u8 blob
# so fixRelocChain has no per-symbol patches to apply here.
