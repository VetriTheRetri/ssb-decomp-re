# Relocation metadata for file 323 (JP variant)
# Format: <type> <ptr_label> <target_label>
# JP build compiles LuigiModel as a single chain-encoded raw u32 blob
# — the chain bytes are already baked into the .c source, so
# fixRelocChain has no per-symbol patches to apply here.
