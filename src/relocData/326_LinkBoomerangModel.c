/* relocData file 326: LinkBoomerangModel — raw blob (pending structural decomposition). */

#include "relocdata_types.h"

u8 dLinkBoomerangModel[0x1D0] = {
	#include <LinkBoomerangModel/LinkBoomerangModel.data.inc.c>
};
